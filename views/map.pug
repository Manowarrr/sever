extends layout

block content

  .row
    .col-md-12
      .card
        .card-header.card-header-text.card-header-rose
          .card-text
            h4.card-title Карта объектов
        .card-body
          div(id='map')